[Unit]
Description=Glances Server
After=network.target

[Service]
EnvironmentFile=/etc/conf.d/glances
ExecStart=/etc/glances/bin/glances --config /etc/glances/glances.conf --export prometheus
Restart=always
RemainAfterExit=no

[Install]
WantedBy=multi-user.target
